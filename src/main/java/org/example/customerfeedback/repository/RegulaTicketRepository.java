package org.example.customerfeedback.repository;

import org.example.customerfeedback.domain.RegulaTicket;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RegulaTicketRepository extends JpaRepository<RegulaTicket, Long> {
}
